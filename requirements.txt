# Core runtime (§9: no paid APIs, no monthly services).
PyYAML>=6.0

# FFmpeg without a system install — bundled binary, offline, cross-platform.
imageio-ffmpeg>=0.5

# YouTube Data API v3 upload (optional; free tier, no monthly fee).
# Without these, publishing falls back to a local manifest.
google-api-python-client>=2.0
google-auth-oauthlib>=1.0

# Dev / demo only: placeholder frames + tests.
Pillow>=10.0
pytest>=7.0
