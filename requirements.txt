# Core runtime (§9: no paid APIs, no monthly services).
PyYAML>=6.0

# FFmpeg without a system install — bundled binary, offline, cross-platform.
imageio-ffmpeg>=0.5

# Dev / demo only: placeholder frames + tests.
Pillow>=10.0
pytest>=7.0
